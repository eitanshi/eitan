# Wiki Schema — 光启 v3.2

> 领域：个人知识管理系统（PKM），涵盖 AI/ML、编程、产品思维、认知科学等任何你输入的观点和思考。
> 目标：将每次对话中的真知灼见固化为可检索、可关联、可复利、**可溯源**的知识资产。

> **v3.2 补丁（2026-08-09，层级体系上线）：**
> 1. 新增 `#层级/` 标签组：元卡/框架/观点/实证/素材 五层，用于 Obsidian 关系图分层着色
> 2. 根目录支持卡片（SCHEMA/index/log/工作流/认知拓扑架构师等）不入层级
> 3. 取代 v3.1 第 4 条"暂不引入 #层级/"——分层需求已成熟

> **v3.1 补丁（2026-08-05，源自 QwenChat「认知层级与信息结构」分支的融合建议，精简版）：**
> 1. 标签体系新增「状态流转」维度（`#状态/梦觉醒悟道`），追踪卡片的认知生命周期
> 2. 线上部分新增固定字段 `## 第一性原理 (The Signal)` —— 一句话（≤50字）收敛该卡片的确定信号
> 3. 「缘起溯源」表格新增「认知状态跃迁」列
> 4. 暂不引入 `#层级/`、`#介质/` 标签组 —— 防止标签扩散，先以状态组试运行（已被 v3.2 取代）

---

## 核心约定

- 文件名：小写 + 连字符（如 `llm-推理链设计.md`）
- 每篇以 YAML frontmatter 开头
- 使用 `[[WikiLink]]` 建立关联（每篇至少 2 个出链）
- 更新页面时必须更新 `updated` 日期
- 每篇新页面必须在 `index.md` 对应分区注册
- 每次操作必须追加到 `log.md`
- **溯源标记**：综合 3+ 来源的页面，在段落末尾追加 `^[raw/xxx.md]` 标记来源
- **观点演化**：当观点跨越多次对话时，使用 `lineage` 字段建立演化链

---

## 设计原则：两层页面

> 受 gbrain"Compiled Truth + Timeline"模式启发（参见 `concepts/gbrain-光启集成架构`）。

每张知识卡片由 `---`（水平分割线）分为两层，回答两个不同的问题：

```
─────────────────────────────────────────────
  线 上 —— 编译真理 (Compiled Truth)
  "当前是什么状态？"
  → 有新信息进来时**重写**，永远保持最新

─────────────────────────────────────────────
  线 下 —— 时间线 (Timeline)
  "这个状态是怎么来的？"
  → **只追加，永不重写**。演化历史只进不出
─────────────────────────────────────────────
```

**为什么分两层：** 光启 v2 把"定义/论点/共识"和"辩论过程/缘起溯源/原始对话"混在同一层。结果是读卡片时"当前结论"和"历史证据"纠缠在一起，难以快速回答"现在怎么想的"vs"之前怎么想的"。两层分离后：
- 读上半部分 = 快速了解当前状态（适合检索/回答"现在结论"）
- 读下半部分 = 理解演化路径（适合溯源/理解"为什么是现在的结论"）

**写入规则：**
- 有新洞察或共识更新 → **重写上半部分**（定义、核心论点、当前共识）
- 有新讨论或事件 → **追加到下半部分**（辩论记录、时间线、原始对话）
- 上半部分的内容可以**精简/改写**，但不能删除——重要的演化必须在下半部分有记录
- `contested: true` 的卡片：上半部分保留当前共识，矛盾双方在下半部分时间线中并置记录

---

## YAML Frontmatter

```yaml
---
title: 知识卡片标题
created: YYYY-MM-DD
updated: YYYY-MM-DD
type: concept | entity | comparison | query | opinion | principle | origin
tags: [来自下方分类]
sources: [raw/...]
lineage: [earlier-card-1, earlier-card-2]   # 🆕 观点演化链，按时间先后排列
confidence: high | medium | low
contested: true    # 当存在未解决分歧时
status: seedling | growing | evergreen   # 知识成熟度
---
```

### lineage 字段说明

`lineage` 记录一个观点的**前世今生**——它是从哪些更早的观点或对话中演化而来的。

- 值是一个 Obsidian wiki link 列表，按时间先后排列
- 第一个是最早的来源，最后一个是最近的前驱
- 如果本卡片是一个全新的原创观点（无前驱），可以省略此字段
- 如果本卡片**修正或推翻了**某个前驱观点，应当标注

```
lineage: [[raw/观点-2026-07-01-初始想法]], [[concepts/信识认知框架]]
```
→ 表示本观点源自「初始想法」→ 经过「信识认知框架」→ 又演化出本卡片

### raw/ 目录 frontmatter

```yaml
---
session_ref: 2026-07-01-观点讨论
source_type: hermes | qwenchat | minimaxchat | external   # 🆕 来源类型
ingested: YYYY-MM-DD
sha256: <body 内容的 SHA256>
---
```

---

## 标签分类

- **认知类型**：concept, principle, opinion, argument, framework, origin
- **领域**：ai, ml, programming, product, philosophy, cognitive-science, system-design, mathematics, history, linguistics, civilization, geometry, i-ching, buddhism
- **状态**：seedling, growing, evergreen
- **方法论**：socratic, dialectic, reductionism, first-principles, system-thinking
- **元标签**：cross-reference, contradiction, open-question, lineage-node
- 🆕 **状态流转**（v3.1）：梦(开环), 觉(误差), 醒(闭环), 悟(重构), 道(稳态) —— 以 `#状态/xx` 形式用于 frontmatter `tags` 或正文，标记卡片当前所处的认知生命周期阶段
- 🆕 **层级**（v3.2）：元卡, 框架, 观点, 实证, 素材 —— 以 `#层级/xx` 形式用于 frontmatter `tags`，每张成品卡恰好一层，用于关系图分层着色：
  - **元卡**：统摄整个认知框架的顶层卡（当前：双核认知框架、点线面体生成序列、认知架构-v2-阴阳框架）
  - **框架**：可复用的结构/协议/方法论/状态机（如信识四字骨架、五步协议、同构表、降熵元目的）
  - **观点**：单个判断/主张/发现（如人vs人类、文化vs文明、五套同构发现）
  - **实证**：可验证的事实主张；已证/待证不单设标签，由 `confidence` + `contested` 字段表达
  - **素材**：raw/ 下日常对话与碎片灵感，图谱中作素材底座
  - **根目录支持卡片不入层级**（SCHEMA/index/log/工作流/认知拓扑架构师/第二大脑设计原则/gbrain 集成）

新标签必须先添加到此分类，再使用。

---

## 页面门槛

- **创建页面**：当概念在 2+ 次讨论中出现，或单次讨论中有深度洞察
- **添加到现有页面**：新观点补充已有知识时
- **不要创建**：随口一提、未形成判断的观点
- **拆分页面**：超过 ~200 行时拆分
- **归档页面**：内容已被完全替代时归档

---

## 概念卡片页格式（v3 两层制）

```markdown
---
title: 卡片标题
type: concept
tags: [...]
lineage: [[...]]
---

═══════════════════════════════════════════
▌  线 上：编译真理（当前状态）
═══════════════════════════════════════════

## 定义

一句话说明。然后展开。

## 第一性原理 (The Signal)

> 🆕 v3.1 固定字段。用一句话（不超过 50 字）或一个极简公式，收敛该卡片的终极确定信号（认知/界）。
> 作用：防止「编译真理」随时间推移再次陷入知识的扩散。有新洞察时随线上部分一起重写。

## 核心论点

当前持有的核心观点。如果有演变，记录的是**最新版本**。

## 当前共识

形成的共识或暂时性结论。当有新信息时，这一节被**重写**而非追加。

## 当前状态（可选）

对于有结构化状态的实体（项目进度、人、公司等），用字段形式：

- **状态**：活跃 / 搁置 / 完成 / ...
- **最后更新**：YYYY-MM-DD
- **关键指标**：...

## 开放问题

当前尚未解决、仍在探讨的问题（从时间线上升起的未结线程）。

═══════════════════════════════════════════
▌  线 下：时间线（证据日志）
═══════════════════════════════════════════

---

## 辩论与演化记录

时间倒序。每次有新讨论，**追加**新条目，不删旧条目。

### YYYY-MM-DD — 讨论主题

- **正方**：
- **反方**：
- **结果**：（被吸收进上半部分共识 / 仍存疑 / 被放弃）
- **来源**：`session` / `[[raw/xxx.md]]`

## 缘起溯源

观点的演化路径。标注本观点从哪些更早的观点演化而来。

| 节点 | 时间 | 来源 | 做了什么 | 认知状态跃迁 |
|:----|:-----|:-----|:---------|:-------------|
| 萌芽 | | | 最初在什么情境下浮现 | 梦 → 觉 |
| 展开 | | | 在哪次对话中被深化 | 觉 → 醒 |
| 成型 | | | 达成共识、形成卡片 | 醒 → 悟 |
| 更新 | YYYY-MM-DD | session | 更新了共识：… | … |

## 原始对话

> 来自 session: [YYYY-MM-DD 讨论主题](...)

---

## 交叉引用（两层共有）

放在下半部分末尾。记录本卡片与知识库中其他卡片的**关系**：

- [[相关页面1]] — 关系说明（如"演化自""同构于""反对""补充"）
- [[相关页面2]]

## 更新策略

当新信息与现有内容冲突时：
1. 检查日期 — 较新的来源优先级更高
2. 如果确实矛盾，上半部分保留当前共识，下半部分时间线并置记录双方立场 + 日期 + 来源
3. 在 frontmatter 标记 `contested: true`
4. 标记为待用户审阅
5. 把旧共识**移入**下半部分时间线，而非删除

## 观点演化追踪

当发现新观点与已有观点有关联时：
1. 在新卡片的 `lineage` 字段中录入已有观点
2. 可选：回到已有观点，在「交叉引用」中添加新卡片
3. 在 `log.md` 中标注演化关系：`evolved_from → evolved_to`
4. 如果演化导致旧卡片结论变更：**重写旧卡片上半部分**，把旧版本移入下半部分时间线
```
