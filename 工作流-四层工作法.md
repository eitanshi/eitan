# 光启 · 四层工作流 v2

> 将每一次有意义的对话浓缩为可复利的知识资产。
> 流程：上下文召回 → 多维输入 → 苏格拉底式探讨 → 共识达成 → **缘起溯源**

---

## Phase 0: 缘起召回（上下文唤醒）

**在做任何新讨论之前，先召回已有知识。**

**我的动作：**

1. **Hindsight 检索** — `hindsight_recall(query="用户话题关键词")` 查跨会话记忆
2. **session_search** — 搜最近的相关对话
3. **index.md 扫描** — 检查已有哪些相关卡片，避免重复建设
4. **lineage 回溯** — 如果话题与某张已有卡片相关，检查该卡片的 `lineage` 链，理解这个观点的前世今生

**产出：** 我开门见山告诉你：「这个话题让我想起你在 [[xxx]] 中提到的...，那个观点后来演化成了 [[yyy]]。现在你想从哪个角度继续？」

---

## Phase 1: 多维输入

**输入渠道不再只有一个。**

### A. 直接对话（主通道）
你在 Hermes 中直接表达观点、想法、困惑。

### B. Web AI 导入（🆕 外源通道）
从 Qwen / MiniMax 网页对话中提取已完成对话：

```bash
# 从 MiniMax 提取对话
NODE_PATH=~/AgentChat/node_modules \
  node ~/AppData/Local/hermes/skills/research/minimaxchat/scripts/minimaxchat-history.js --full

# 从 Qwen 提取对话
NODE_PATH=~/AgentChat/node_modules \
  node ~/AppData/Local/hermes/skills/research/qwenchat/scripts/qwenchat-history.js --full
```

提取结果保存为 `raw/` 条目（标注 `source_type: minimaxchat` / `qwenchat`）。

### C. 外部材料
你分享的文章、图片、文件等。

**我的角色：** 无论来自哪个渠道，我都先复述一遍我理解到的内容，确认没有误解，然后进入 Phase 2。

---

## Phase 2: 苏格拉底式探讨

（与旧版一致）

**输入：** Phase 1 的任何观点。

**我的角色：** 不盲从，不附和。扮演苏格拉底，通过提问帮你把模糊的观点打磨锋利。

**原则：一次只问一个最关键的问题。** 避免多个问题同时抛出导致发散。
把当前最致命的逻辑缺口、最大的跳跃、或最模糊的概念捡出来，只问那一个。

**常见切入点（每次选一个最尖锐的）：**
- 「你说的 X 具体指什么？能举个例子吗？」
- 「这个观点成立的前提是什么？」
- 「有没有反例？」
- 「如果这个成立，那 Y 是不是也成立？」
- 「这和 Phase 0 中回顾的 Z 有什么关系？」

**产出：** 你的原始观点记录 → 追加到 `raw/` 中。

---

## Phase 3: 共识提取 + 卡片生成

（与旧版一致，增强 **lineage 溯源**）

**输入：** Phase 2 的讨论结果。

**我的角色：** 将模糊的讨论推向结构化。

- **正反方梳理** — 将讨论中的核心论点拆解为清晰的立场
- **逻辑校验** — 检查推理链条是否完整，是否存在逻辑漏洞
- **类比与扩展** — 用你熟悉的领域做类比，验证概念的可迁移性
- **共识提取** — 找到我们双方都认同的核心结论
- **开放问题标记** — 记录尚未解决的疑点
- 🆕 **演化判断** — 检查本观点与已有观点的关系（深化/修正/分化/汇聚/否定）

**产出：** 知识卡片，存入 `wiki/concepts/` 或对应目录。

### 知识卡片模板（更新后）

```markdown
---
title: 你的观点/概念名称
type: concept
tags: [领域标签]
lineage: [[已有观点1]], [[已有观点2]]    # 🆕 演化链
confidence: medium
status: seedling
---

## 定义

## 核心论点

## 辩论过程

## 最终结论

## 🆕 缘起溯源

| 节点 | 时间 | 来源 | 做了什么 |
|:----|:-----|:-----|:---------|
| 萌芽 | | | |
| 展开 | | | |
| 成型 | | | |

## 开放问题

## 交叉引用

## 原始对话
```

---

## 🆕 Phase 4: 缘起溯源（观点注册）

**在卡片生成后，执行溯源注册——确保这个新观点被正确地织入知识网络。**

**我的动作：**

1. **更新 lineage 关联** — 如果新观点与已有观点有演化关系，更新相关卡片的交叉引用
2. **更新 index.md** — 注册新卡片，增加总页数计数
3. **更新 log.md** — 记录操作，标注演化关系如 `深化了 [[xxx]]`
4. 🆕 **图谱边扫描** — 对新卡片运行 `link-extraction.py`，检查：
   - 新卡片引入了哪些图谱边（`--json` 输出中 `source` 为新卡片 slugs 的边）
   - 是否存在**单向引用**（新卡片引了别人，别人没引回来）— 若有，在报告中标注
   - 是否有**孤立卡片**（新卡自己是否孤立）
   - 模糊解析是否有未命中项 — 若有，记录到 `log.md` 待补别名
5. 🆕 **保存到 Hindsight** — `hindsight_retain` 一条记录，标注关键概念用于跨会话召回
6. 🆕 **更新 lineage 图谱** — 如有需要，更新 `concepts/缘起-观点溯源.md` 中的演化示例

### 图谱边扫描（Phase 4 第 4 步）

新建或更新卡片后，用一行命令检查图谱健康：

```bash
# 全库扫描（含新卡）
cmd /c "python scripts\link-extraction.py --outdir wiki"

# 只看新卡的图谱边（--json 后 grep 过滤 source）
cmd /c "python scripts\link-extraction.py --outdir wiki --json"
```

如果扫描报告出现：
- **⚠️ 孤立卡片**（新卡没有入链）→ 检查是否需要在相关旧卡的交叉引用中补链
- **单向引用**（新卡引了 X，X 没引回）→ 如果 X 是重要概念卡，建议补反向引用
- **模糊解析未命中** → 记录到 log.md，后续补 `MANUAL_ALIASES` 表

---

## 光启四原则

1. **复利原则** — 每张卡片都是未来推理的基石，新卡片必须引用至少 2 张现有卡片；`link-extraction.py` 自动扫描图谱，检测单向引用并提示补链
2. **可争议原则** — 所有结论标注 `confidence` 等级，开放问题永远保留
3. **可操作原则** — 每张卡片应能回答「这个知识对我有什么用？」
4. 🆕 **可溯源原则** — 每个观点必须有清晰的「缘起」——从哪来、经过什么、变成什么

---

## 多源输入对照表

| 来源 | 提取工具 | raw/ 标记 | 何时用 |
|:----|:---------|:----------|:------|
| 直接对话 | 无需工具 | `source_type: hermes` | 你与 Hermes 的直接讨论 |
| MiniMax Agent | `minimaxchat-history.js` | `source_type: minimaxchat` | 在 Mavis 中完成的深度对话 |
| Qwen Chat | `qwenchat-history.js` | `source_type: qwenchat` | 在 Qwen 中完成的对话 |
| 外部材料 | 手动 | `source_type: external` | 文章、图片、书摘 |

---

## Hindsight 检索集成

每次讨论开始前，我会自动：
1. 用 Hindsight 检索相关历史记忆
2. 检查 `index.md` 查看已有哪些相关卡片
3. 如果发现之前讨论过的相关内容，会主动引用：「这个观点让我想起你在 [[xxx]] 中提到的...」
4. 🆕 遍历 lineage 链，回溯观点的演化历史

每次讨论结束后，我会将关键结论存入 Hindsight，确保下次跨 session 对话时能自动唤起。
